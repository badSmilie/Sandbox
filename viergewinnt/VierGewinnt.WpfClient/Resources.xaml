<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VierGewinnt.WpfClient">
    <local:Farbkonverter x:Key="Farbkonverter" />
    <local:IstDranFarbkonverter x:Key="IstDranFarbkonverter" />

    <DataTemplate x:Key="SpielerViewModelDataTemplate" DataType="{x:Type local:ISpielerViewModel}">
        <local:SpielerView />
    </DataTemplate>
    <DataTemplate x:Key="SpielbrettViewModelDataTemplate" DataType="{x:Type local:ISpielbrettViewModel}">
        <local:SpielbrettView />
    </DataTemplate>
</ResourceDictionary>